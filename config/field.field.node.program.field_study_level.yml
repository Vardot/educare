langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_study_level
    - node.type.program
    - taxonomy.vocabulary.study_level
id: node.program.field_study_level
field_name: field_study_level
entity_type: node
bundle: program
label: 'Study level'
description: 'Whether this is an undergraduate or graduate program. Drives the catalog toggle.'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:taxonomy_term'
  handler_settings:
    target_bundles:
      study_level: study_level
    sort:
      field: weight
      direction: ASC
    auto_create: false
    auto_create_bundle: ''
field_type: entity_reference
